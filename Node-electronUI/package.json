{
  "name": "cardano-mine-ui",
  "version": "1.0.0",
  "description": "Cardano community admin and end-user tools with Electron",
  "main": "electron.js",
  "homepage": "./",
  "author": "Cardano Community",
  "license": "MIT",
  "scripts": {
    "start": "node run-app.js",
    "dev": "concurrently \"npm run watch\" \"sleep 3 && npm run start\" 2>/dev/null",
    "dev:cli": "npm run build && echo '✅ Build complete. CLI tools ready:' && echo 'npm run cli:verify -- --help' && echo 'npm run cli:challenge -- --help' && echo 'npm run cli:batch -- --help' && echo 'npm run cli:report -- --help'",
    "build": "webpack --mode development",
    "watch": "webpack --mode development --watch",
    "pack": "electron-builder --dir",
    "dist": "electron-builder",
    "cli:verify": "node cli/verify.js",
    "cli:challenge": "node cli/challenge.js",
    "cli:batch": "node cli/batch-action.js",
    "cli:report": "node cli/report.js",
    "test": "jest",
    "lint": "eslint ."
  },
  "dependencies": {
    "uuid": "^9.0.0",
    "sodium-plus": "^0.8.0",
    "bip39": "^3.1.0",
    "cardano-addresses": "^3.12.0"
  },
  "devDependencies": {
    "electron": "^27.0.0",
    "electron-builder": "^24.6.4",
    "electron-is-dev": "^2.0.0",
    "webpack": "^5.89.0",
    "webpack-cli": "^5.1.4",
    "style-loader": "^3.3.0",
    "css-loader": "^6.8.0",
    "concurrently": "^8.2.0",
    "jest": "^29.7.0",
    "eslint": "^8.50.0"
  }
}
